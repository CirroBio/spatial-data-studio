# Cirro upload configuration. Strictly additive: dark (upload button hidden) unless
# all three are set. Service-account (OAuth client-credentials) identity — no
# interactive login.
CIRRO_BASE_URL=
CIRRO_CLIENT_ID=
CIRRO_CLIENT_SECRET=
