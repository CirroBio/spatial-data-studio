# Cirro upload configuration. Each user signs into Cirro with their own account from
# the browser (OAuth device code), so the server holds no Cirro credential. This only
# prefills the domain field in the "Connect to Cirro" dialog; a user can type any
# other Cirro domain there. Defaults to app.cirro.bio when unset.
CIRRO_BASE_URL=
