{
  "schema_version": 1,
  "meta": {
    "name": "Ligand-receptor interactions",
    "description": "Permutation test (CellPhoneDB-style) for ligand-receptor pairs that are co-expressed across region pairs, grouped by obs['cluster']; results land in uns['cluster_ligrec'] and are shown as a dotplot.",
    "provenance": "squidpy 'Receptor-ligand analysis' example (squidpy.readthedocs.io/en/stable/notebooks/examples/graph/compute_ligrec.html). Targets the visium_hne example with an explicit set of known mouse-brain ligand-receptor pairs."
  },
  "readme": "Runs squidpy's re-implementation of the CellPhoneDB permutation test on an explicit list of mouse-brain ligand-receptor pairs, grouped by `obs['cluster']`, then plots the significant pairs. Interactions are hard-coded (not fetched from omnipath) so the step runs offline; the gene names are specific to the mouse-brain panel. `use_raw` is false because the app's extracted table has no `.raw`.",
  "steps": [
    { "namespace": "gr", "function": "ligrec", "params": {
        "cluster_key": "cluster",
        "interactions": [["Camk2a", "Grin1"], ["Slc17a7", "Grin2b"], ["Bdnf", "Ntrk2"]],
        "n_perms": 100, "use_raw": false, "seed": 0, "show_progress_bar": false } },
    { "namespace": "pl", "function": "ligrec", "params": { "cluster_key": "cluster" } }
  ]
}
