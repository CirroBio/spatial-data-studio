{
  "schema_version": 1,
  "meta": {
    "name": "Cluster hierarchy & markers",
    "description": "Build a hierarchical dendrogram relating the Leiden clusters (uns['dendrogram_leiden']), then rank and filter marker genes per cluster (Wilcoxon). Needs an obs['leiden'] column (present on visium_hne, or run a preprocessing recipe first).",
    "provenance": "scanpy clustering / marker-gene tutorial (sc.tl.dendrogram + sc.tl.rank_genes_groups + sc.tl.filter_rank_genes_groups)."
  },
  "readme": "Orders the Leiden clusters into a dendrogram by expression similarity, then ranks marker genes per cluster with the Wilcoxon test and keeps the significant ones. Requires `obs['leiden']` — present on visium_hne, or run a preprocessing recipe first. Extends 'Marker genes per cluster' with the between-cluster hierarchy.",
  "steps": [
    { "namespace": "sc.tl", "function": "dendrogram", "params": { "groupby": "leiden" } },
    { "namespace": "sc.tl", "function": "rank_genes_groups", "params": { "groupby": "leiden", "method": "wilcoxon" } },
    { "namespace": "sc.tl", "function": "filter_rank_genes_groups", "params": {} }
  ]
}
