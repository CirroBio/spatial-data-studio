{
  "schema_version": 1,
  "meta": {
    "name": "Neighborhood enrichment",
    "description": "Which brain regions border each other more (or less) than chance, from a permutation test over the spatial graph.",
    "provenance": "Adapted from the squidpy 'Neighborhood enrichment' vignette; targets the visium_hne example (hexagonal grid, obs['cluster'] brain-region annotation)."
  },
  "readme": "Which brain regions border each other more (or less) than chance, from a permutation test over the spatial graph. Builds a hexagonal-grid spatial graph, then runs the neighborhood-enrichment permutation test grouped by `obs['cluster']`.",
  "params": [
    { "name": "cluster_key", "schema": { "type": "string", "default": "cluster" }, "widget": "obs_categorical", "bound_to": null, "required": true, "tooltip": "obs column holding the region/cluster labels to test" },
    { "name": "coord_type", "schema": { "type": "string", "enum": ["grid", "generic"], "default": "grid" }, "widget": "select", "bound_to": null, "required": false, "tooltip": "spatial-graph geometry: grid for Visium hexagons, generic otherwise" },
    { "name": "n_neighs", "schema": { "type": "integer", "default": 6 }, "widget": "number", "bound_to": null, "required": false, "tooltip": "neighbors per node in the spatial graph (6 = Visium hexagonal grid)" }
  ],
  "steps": [
    { "namespace": "gr", "function": "spatial_neighbors", "params": { "coord_type": { "$param": "coord_type" }, "n_neighs": { "$param": "n_neighs" } } },
    { "namespace": "gr", "function": "nhood_enrichment", "params": { "cluster_key": { "$param": "cluster_key" }, "seed": 0, "show_progress_bar": false } },
    { "namespace": "pl", "function": "nhood_enrichment", "params": { "cluster_key": { "$param": "cluster_key" } } }
  ]
}
