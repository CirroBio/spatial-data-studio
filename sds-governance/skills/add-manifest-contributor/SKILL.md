# Skill: add-manifest-contributor

*Retired.* The data manifest subsystem (`backend/app/manifest/`) was removed —
session state is inspected directly through the data inspector and the
element/table APIs (DESIGN.md §13 "Data manifest (removed)"). There is no
manifest to contribute to, so this playbook has no steps and satisfies no rule.
